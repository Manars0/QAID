We are replacing the autogenerated demo dataset with our own ERP demo dataset.

IMPORTANT:
Do NOT generate fake accounting data anymore.
Do NOT generate random dashboard values.

==================================================
DEMO DATASET
==================================================

I will upload our complete ERP demo dataset.

Place it under:

backend/demo_data/

The dataset contains multiple ERP tables such as:

- Approval_Log
- Audit_Log
- Chart_of_Accounts
- Companies
- Cost_Centers
- Currencies
- Customers
- Departments
- Fraud_Labels
- Journal_Header
- Journal_Lines
- Payment_Entries
- Purchase_Invoices
- Sales_Invoices
- Users
- Vendors

==================================================
DEMO MODE
==================================================

When the user clicks "Explore Demo",

the backend must execute EXACTLY the same pipeline used for uploaded files.

Do NOT use any mock JSON.

Do NOT bypass the backend.

The flow must be:

Load Demo Dataset

↓

Read all Excel files

↓

Identify ERP tables automatically

↓

Validate accounting data

↓

Merge related tables

↓

Feature Engineering

↓

Rule Engine

↓

Isolation Forest

↓

Calculate Fraud Probability

↓

Calculate Final Risk Score

↓

Generate Executive Summary

↓

Generate Dashboard

↓

Generate Reports

Demo Mode and Upload Mode must share the same backend code.

The ONLY difference is where the files come from.

==================================================
FRAUD LABELS
==================================================

The file "Fraud_Labels" is our ground-truth dataset.

It represents the known fraud cases that were intentionally embedded into the demo data.

Use it ONLY inside the backend.

Do NOT expose it directly to users.

Do NOT show "Confirmed Fraud" or "Ground Truth" in the UI.

Instead:

- Use it to validate the Rule Engine.
- Use it to evaluate Isolation Forest predictions.
- Use it internally to improve recommendations.
- Use it when generating the Executive Summary.

The dashboard should continue displaying only:

- Risk Score
- High Risk Entries
- Fraud Indicators
- Flagged Accounts
- Recommendations
- Executive Summary

Keep the interface clean and professional.

==================================================
FILE DETECTION
==================================================

The Dataset Loader must automatically detect ERP exports.

It should not rely on exact filenames.

Instead, recognize tables intelligently based on sheet names, columns and metadata.

If some optional tables are missing,

continue the analysis without crashing.

==================================================
CODE QUALITY
==================================================

Create a reusable Dataset Loader service.

Keep everything modular.

Do not duplicate code between Demo Mode and Upload Mode.

Both modes must call the exact same analysis service.

==================================================
BRANDING
==================================================

Update the Arabic branding everywhere.

Arabic Name:

قيد

NOT:

قائد

The name refers to an Accounting Journal Entry.

==================================================
TYPOGRAPHY
==================================================

Use these fonts globally.

English:

Inter

Arabic:

Tajawal

Apply them consistently across:

- Landing Page
- Dashboard
- Reports
- Tables
- Navigation
- Buttons
- Forms
- PDF Reports (where supported)

==================================================
GOAL
==================================================

QAID should behave exactly like a real enterprise ERP Risk & Fraud platform.

Every dashboard, KPI, chart, report and recommendation must be generated from the actual uploaded data or the real demo dataset.

No fake values.
No mock analytics.
No hardcoded dashboard numbers.